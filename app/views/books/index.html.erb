<h1>Livros</h1>

<div id="livros">
  <% @books.each do |book| %>
    <div>
      <%= link_to book.title, "books/#{book.id}" %>
    </div>
  <% end %>
</div>

<%= link_to "Novo Livro", "books/new" %>

